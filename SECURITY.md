# 测试资产安全说明

## 已发现风险

测试页左侧交互日志曾显示视觉服务的 JWT 类 Token。该类凭据不应出现在浏览器可见日志、截图、导出文件或公开仓库中。

## 本仓库的处理

- 不提交测试页的原始全量日志、截图、网络抓包或提示词转储。
- 根因报告只保留路由标签、置信度、耗时、工具结果和脱敏后的业务字段。
- 不提交服务器连接指南、私钥、口令、Cookie、Authorization 请求头或 Token。

## 上线前处理

1. 立即轮换测试期间可能暴露的 Token。
2. 服务端日志采用字段白名单，默认屏蔽 `Authorization`、`Cookie`、`token`、`secret` 和 `key`。
3. 浏览器测试页仅展示请求 ID、阶段、耗时、脱敏参数和状态码。
4. 生产环境关闭完整系统提示词和用户上下文展示。
5. 在 CI 中增加密钥扫描，并阻止含高熵凭据的提交。

如需提交原始日志，应先离线脱敏并由安全负责人复核。
