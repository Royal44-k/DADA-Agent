# 来源与版本

分析报告日期：2026-08-19。归档日期：2026-09-13。统计是当前用户提供目录的文件快照，不是整个 Git 历史，也不是服务器运行规模。

## 源码快照

| 原目录名 | 文件数 | 原始字节 | ZIP 字节 | 本地 Git HEAD |
| --- | ---: | ---: | ---: | --- |
| grok_build | 2,737 | 57,376,068 | 13,710,801 | `8adf9013a0929e5c7f1d4e849492d2387837a28d` |
| deepseek_harness | 7,412 | 47,679,644 | 17,907,186 | `47f943859bef60e4160492346772ded9b24f765a` |
| pi_build | 1,074 | 15,967,655 | 6,154,421 | `c8c3cd499f4d35c0f9cfebfec5f4e3822411a49f` |

合计 **11,223 个源文件**（121,023,367 字节），包含两个未被 Git 跟踪的中文调研笔记。文件内容未做格式化或修复，逐文件大小与 SHA-256 见 `manifests/*.json`。ZIP 顶层目录与原目录同名，记录源 Git 文件模式，成员时间统一为归档日期，不能用 ZIP 时间推断源文件修改时间。

## Grok 的双版本标识

- 本地公开源码树 Git HEAD：`8adf9013a0929e5c7f1d4e849492d2387837a28d`。
- 源目录 `SOURCE_REV`：`2ec0f0c8488842da03a71eeee3c61154957ca919`。
- 原 README 说明 `SOURCE_REV` 是该代码同步自 monorepo 的提交。原 HTML 报告引用的是这个值，不是本地 Git HEAD；两者不同不代表本次修改了源码。

DeepSeek/Pi 归档 HEAD 与其报告引用提交一致。报告生成后没有在本次更新为上游最新版本。

## 原始成果资产

- 3 个 HTML：Grok、DeepSeek Harness、Pi Agent。
- 12 个 PNG：每个项目 4 个原报告预览。
- 1 个原始 `.mjs`：DeepSeek/Pi 浏览器检查脚本。
- 2 份原目录中文笔记额外复制到 `source-notes/` 便于浏览，已包含在上述源码文件计数中，不能再次相加。
- 5 份根许可证/notices 额外复制到 `licenses/`，也已包含在源码快照中。

以上 16 份原始成果资产保持字节一致。新增 README、对照摘要、来源说明、清单和离线核验工具是归档整理材料；报告/截图合集只重复打包现有报告与预览，不是新增研究结论。

## 选择与排除口径

来源限于用户点名的 `grok_build`、`deepseek_harness`、`pi_build` 及本次对应生成文件。Git 跟踪文件加未被忽略的附加文件纳入快照；排除本地 VCS 数据、依赖、缓存和 ignored 生成文件。剪枝目录只记一个目录条目，其下文件数没有展开。完整排除项见 [EXCLUSIONS.json](../manifests/EXCLUSIONS.json)。

原始目录未被修改；不含其他任务的 Cordis 插件工程报告、评测数据或生产服务器资产。
